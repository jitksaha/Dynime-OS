/**
 * Legacy re-exports — all loading is now skeleton-based.
 * Keeping these export names so existing imports don't break.
 */
export { PageSkeleton as PageLoader, AuthSkeleton as AuthLoader, PageSkeleton as SectionLoader } from "@/components/PageSkeleton";
